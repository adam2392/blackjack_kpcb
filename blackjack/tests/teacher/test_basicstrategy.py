import pytest

from blackjack.teacher.base import BasicStrategy

class Test_BasicStrategy():

    def test_bs(self):
        bs = BasicStrategy()

        # bs.suggest_action(house, player)
        assert False
